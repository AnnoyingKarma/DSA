#include <iostream>
using namespace std;

int numberOfSubstrings(string s){
  int len = s.length();
  
  return 0;
}

int main(){
  string s = "abcabc";
  // cout << numberOfSubstrings(s);
  int len = s.length();
  
}